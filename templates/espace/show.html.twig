{% extends 'client/dashboardClient.html.twig' %}

{% block client %}
    <h1>Espace</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ espace.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ espace.nom }}</td>
            </tr>
            <tr>
                <th>Caracteristique</th>
                <td>{{ espace.caracteristique }}</td>
            </tr>
           <tr>
                <th>image</th>
                {# <a href="{{ asset('uploads/brochures/' ~ product.brochureFilename) }}">View brochure (PDF)</a> #}
                <td>
                    <img  src="{{ asset('Back/img/' ~ espace.image )  }}" alt="" width="80" height="80" />
                    {# {{ asset('Back/img' ~ evenement.image)  }} #}
                </td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ espace.adresse }}</td>
            </tr>
            <tr>
                <th>Dispo</th>
               <td>{{ espace.dispo }}</td>
            </tr>
            <tr>
                <th>Tarif horaire</th>
                <td>{{ espace.tarifhoraire ? espace.tarifhoraire|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
           
            
            <tr>
                <th>Prix location</th>
                <td>{{ espace.prixlocation }}</td>
            </tr>
            <tr>
                <th>Catégorie</th>
                <td>{{ espace.categorielocation.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_espace_index') }}">back to list</a>

    <a href="{{ path('app_espace_edit', {'id': espace.id}) }}">edit</a>

    {{ include('espace/_delete_form.html.twig') }}
{% endblock %}
