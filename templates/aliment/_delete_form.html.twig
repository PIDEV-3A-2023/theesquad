<form method="post" action="{{ path('app_aliment_delete', {'id': aliment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aliment.id) }}">
    <button class="btn">Delete</button>
</form>
