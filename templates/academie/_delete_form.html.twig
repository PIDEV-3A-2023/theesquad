<form method="post" action="{{ path('app_academie_delete', {'id': academie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ academie.id) }}">
    <button class="btn">Delete</button>
</form>
